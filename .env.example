# Database Configuration
DATABASE_URL=postgres://username:password@host:port/databasename

# Environment Mode
NODE_ENV=development # Change to 'production' for deployments

# Session Security (Generate a strong random string)
SESSION_SECRET=your_random_secure_string_here

# Other Optional Settings
# PORT=5000 # Override default port if needed

# Mapbox API Token
VITE_MAPBOX_TOKEN=your_mapbox_token_here